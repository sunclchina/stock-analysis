services:
  stock-analysis:
    image: stock-analysis:latest
    container_name: stock-analysis
    restart: always
    ports:
      - "8000:8000"
    env_file:
      - D:/docker/stock-analysis/.env.vars
    volumes:
      - D:/docker/stock-analysis/data:/app/data
    healthcheck:
      test: ["CMD-SHELL", "curl -f http://localhost:8000/api/v1/health || exit 1"]
      interval: 30s
      timeout: 3s
      start_period: 15s
      retries: 3
